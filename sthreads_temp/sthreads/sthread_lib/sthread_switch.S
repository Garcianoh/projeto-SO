/*                                                          Emacs: -*- c -*-
 *
 * This file is just a place holder for the real assembly, which is a in a
 * cpu-specific file we conditionally include.
 */

#include <config.h>

/* Troca de contexto agora é feita via setjmp/longjmp em C. Este arquivo é mantido apenas por compatibilidade. */
